<header class="bg-red-100 text-gray-900 shadow p-4 mb-6">
    <h1 class="text-2xl font-bold">TrailTracker</h1>
  
    <nav class="mt-2">
      <ul class="flex space-x-4">
        <li>
          <a href="/" class="<%= currentPage === 'home' ? 'text-yellow-700 font-semibold' : 'text-black hover:text-yellow-600 transition' %>">Home</a>
        </li>
        <li>
          <a href="/auth/signup" class="<%= currentPage === 'signup' ? 'text-yellow-700 font-semibold' : 'text-black hover:text-yellow-600 transition' %>">Sign Up</a>
        </li>
        <li>
          <a href="/auth/login" class="<%= currentPage === 'login' ? 'text-yellow-700 font-semibold' : 'text-black hover:text-yellow-600 transition' %>">Login</a>
        </li>
      </ul>
    </nav>
  </header>